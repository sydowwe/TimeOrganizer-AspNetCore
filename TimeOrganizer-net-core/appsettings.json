{
  "AppSettings": {
    "Name": "TimeOrganizer",
    "Version": "v1.0"
  },
  "ConnectionStrings": {
    "DefaultConnection": "Host=srv544364.hstgr.cloud;Username=jakub;Password=u9TwA2dUjhQS9sw%;Database=asp_net_test"    
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "MailSettings": {
    "SmtpServer": "${MAIL_SMTP_SERVER}",
    "SmtpPort": "${MAIL_SMTP_PORT}",
    "SmtpUsername": "${MAIL_SMTP_USERNAME}",
    "SmtpPassword": "${MAIL_SMTP_PASSWORD}",
    "EnableSsl": "${MAIL_ENABLE_SSL}",
    "TrustServerCertificate": "${MAIL_TRUST_SERVER_CERTIFICATE}"
  },
  "OAuth2": {
    "Google": {
      "ClientId": "${OAUTH2_GOOGLE_CLIENT_ID}",
      "ClientSecret": "${OAUTH2_GOOGLE_CLIENT_SECRET}",
      "RedirectUri": "${OAUTH2_GOOGLE_REDIRECT_URI}"
    }
  }, 
  "DbSettings": {
    "DDLType": "${DB_DDL_TYPE}"
  }
}
